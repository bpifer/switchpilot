# SwitchPilot Kubernetes manifests (namespace: switchpilot)
# Create switchpilot-secrets first — see docs/DEPLOYMENT.md.
apiVersion: apps/v1
kind: Deployment
metadata: { name: db, labels: { app: db } }
spec:
  replicas: 1
  selector: { matchLabels: { app: db } }
  template:
    metadata: { labels: { app: db } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: switchpilot }
            - { name: POSTGRES_USER, value: switchpilot }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: switchpilot-secrets, key: POSTGRES_PASSWORD } }
          ports: [{ containerPort: 5432 }]
          volumeMounts: [{ name: pgdata, mountPath: /var/lib/postgresql/data }]
      volumes:
        - name: pgdata
          persistentVolumeClaim: { claimName: switchpilot-pgdata }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: switchpilot-pgdata }
spec:
  accessModes: [ReadWriteOnce]
  resources: { requests: { storage: 20Gi } }
---
apiVersion: v1
kind: Service
metadata: { name: db }
spec:
  selector: { app: db }
  ports: [{ port: 5432 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: redis, labels: { app: redis } }
spec:
  replicas: 1
  selector: { matchLabels: { app: redis } }
  template:
    metadata: { labels: { app: redis } }
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports: [{ containerPort: 6379 }]
---
apiVersion: v1
kind: Service
metadata: { name: redis }
spec:
  selector: { app: redis }
  ports: [{ port: 6379 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: api, labels: { app: api } }
spec:
  replicas: 2   # safe to scale: cron sweeps are guarded by a Postgres advisory lock (leader election)
  selector: { matchLabels: { app: api } }
  template:
    metadata: { labels: { app: api } }
    spec:
      containers:
        - name: api
          image: switchpilot-api:latest   # replace with your registry path
          env:
            - { name: POSTGRES_HOST, value: db }
            - { name: POSTGRES_DB, value: switchpilot }
            - { name: POSTGRES_USER, value: switchpilot }
            - { name: REDIS_URL, value: "redis://redis:6379" }
            - { name: ENABLE_API_DOCS, value: "false" }   # hide the API schema in prod
            # - { name: ALLOWED_ORIGINS, value: "https://switchpilot.your-domain" }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: switchpilot-secrets, key: POSTGRES_PASSWORD } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: switchpilot-secrets, key: JWT_SECRET } }
            - name: CREDENTIAL_KEY
              valueFrom: { secretKeyRef: { name: switchpilot-secrets, key: CREDENTIAL_KEY } }
          ports: [{ containerPort: 3000 }]
          volumeMounts: [{ name: firmware, mountPath: /data/firmware }]
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "1000m", memory: "512Mi" }
          readinessProbe:
            httpGet: { path: /api/health, port: 3000 }
            initialDelaySeconds: 10
          livenessProbe:
            httpGet: { path: /api/health, port: 3000 }
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      volumes:
        - name: firmware
          persistentVolumeClaim: { claimName: switchpilot-firmware }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: switchpilot-firmware }
spec:
  accessModes: [ReadWriteOnce]
  resources: { requests: { storage: 50Gi } }
---
apiVersion: v1
kind: Service
metadata: { name: api }
spec:
  selector: { app: api }
  ports: [{ port: 3000 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: web, labels: { app: web } }
spec:
  replicas: 2
  selector: { matchLabels: { app: web } }
  template:
    metadata: { labels: { app: web } }
    spec:
      containers:
        - name: web
          image: switchpilot-web:latest   # replace with your registry path
          ports: [{ containerPort: 80 }]
---
apiVersion: v1
kind: Service
metadata: { name: web }
spec:
  selector: { app: web }
  ports: [{ port: 80 }]
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: switchpilot
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 2g
spec:
  rules:
    - host: switchpilot.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: web, port: { number: 80 } } }
